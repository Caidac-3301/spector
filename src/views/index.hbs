{{!-- DUMMY GRAPHS FOR DEMO --}}

<div class="mt-4 row">
  <div class="card col-6" style="width: 90%">
    <div class="card-header">
      <h3 class="card-title">
        <span class="text-right pr-4 mr-4">Function Invocations (invocations/20s)</span>
        <span class="pl-4 ml-4 text-muted text-right">DUMMY</span>
      </h3>
    </div>
    <div class="card-body">
      <div id="chart-wrapper-1" style="height: 16rem"></div>
    </div>
  </div>
  <div class="card col-6">
    <div class="card-header">
      <h3 class="card-title">
        <span class="text-right pr-4 mr-4">Mean Invocation Duration (ms/20s)</span>
        <span class="pl-4 ml-4 text-muted text-right">DUMMY</span>
      </h3>
    </div>
    <div class="card-body">
      <div id="chart-wrapper-2" style="height: 16rem"></div>
    </div>
  </div>
</div>

<script>
  require(['c3', 'jquery'], function (c3, $) {
    $(document).ready(function () {
      var chart = c3.generate({
        bindto: '#chart-wrapper-1', // id of chart wrapper 1
        data: {
          columns: [
            // each columns data
            ['data1', 67, 107, 45, 10, 84, 70],
            ['data2', 4, 0, 14, 22, 1, 2]
          ],
          labels: true,
          type: 'line', // default type of chart
          colors: {
            'data1': tabler.colors["green"],
            'data2': tabler.colors["red"]
          },
          names: {
            // name of each serie
            'data1': 'Success',
            'data2': 'Error'
          }
        },
        axis: {
          x: {
            type: 'category',
            // name of each category
            categories: ['00:45', '01:30', '02:15', '03:00', '03:45', '04:30']
          },
        },
        legend: {
          show: false, //hide legend
        },
        padding: {
          bottom: 0,
          top: 0
        },
      });

      var chart = c3.generate({
        bindto: '#chart-wrapper-2', // id of chart wrapper 2
        data: {
          columns: [
            // each columns data
            ['data1', 5500, 2000, 1700, 11000, 800, 1500],
            ['data2', 0, 0, 3000, 5000, 2000, 300]
          ],
          labels: true,
          type: 'bar', // default type of chart
          colors: {
            'data1': tabler.colors["azure"],
            'data2': tabler.colors["gray"]
          },
          names: {
            // name of each serie
            'data1': 'Function',
            'data2': 'Service'
          }
        },
        axis: {
          x: {
            type: 'category',
            // name of each category
            categories: ['00:45', '01:30', '02:15', '03:00', '03:45', '04:30']
          },
        },
        legend: {
          show: false, //hide legend
        },
        padding: {
          bottom: 0,
          top: 0
        },
      });
    });
  });
</script>